<%= form_for [@question, @answer] do |f| %>
<div>
  <%= f.label :content %><br>
  <%= f.text_area :content %>
</div>
  <div>
    <%= f.submit "Submit Answer" %>
  </div>
<% end %>

<%= link_to "Back to Question", question_path(@question) %>

